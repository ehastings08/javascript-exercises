var snakeCase = function(word) {


  // Find upper case letter and split the word into two at that index
  /*
  for (var i=1; i<word.length; i++) {
    if (word[i] == word[i].toUpperCase()) {
      upperCharIndex = i;
    }
  }

  if (typeof upperCharIndex !== undefined) {

  }
  */
}

module.exports = snakeCase
